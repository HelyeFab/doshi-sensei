1. After every deployment app on mobile is stuck when launching only deleting storage and cache works
2. Check What's the app behaviour when there is no internet and what sort of message we display
4. IMG 01-02 when displaying a modal we can get rid of the navbar in mobile view. This behaviour should be implemented throughout the app

8. Check if for some sort of bug the app companion is displayed twice with the one in the foreground overlapping the one behind. During the app companion animation I can see almost the second one behind
9.


20. Revisit the whole app and it's features and how users can use it to accompany their learning experience so that the onboarding tutorial can be updated
21. Check the settings page to see if all the pages like policy acknowledgement etc are properly implemented with proper text
22. Check why forms on netlify
